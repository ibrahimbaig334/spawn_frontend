# Spawn UI environment
#
# Copy to .env.local and fill in before running the app.

# thirdweb storage — client-side logo uploads (frontend owns the upload)
# Get keys at https://thirdweb.com/dashboard/settings/api-keys
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=
NEXT_PUBLIC_THIRDWEB_SECRET_KEY=
